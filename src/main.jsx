import React from "react";
import ReactDOM from "react-dom/client";

import ClassQuiz from "./classQuiz/classQuiz.jsx";
ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ClassQuiz></ClassQuiz>
  </React.StrictMode>
);
